# EICH 2 — Python Dependencies
# Phase 1 MVP

# Core
python-dotenv>=1.0.0          # environment variable loading in settings.py

# Data / parsing
lxml>=5.0.0                   # XBRL XML parsing in m3_structured_extractor
pdfplumber>=0.11.0             # PDF table and text extraction
openpyxl>=3.1.0               # CSV/Excel registry parsing

# AI extraction
openai>=1.0.0                 # AI evidence extractor (bounded prompts)

# Validation / schema
pydantic>=2.0.0               # optional runtime validation layer (supplement to dataclasses)

# Serialisation
orjson>=3.9.0                 # fast JSON serialisation for report output

# Testing
pytest>=8.0.0
pytest-cov>=5.0.0
